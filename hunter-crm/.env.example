# Postgres connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=huntercrm
PGUSER=postgres
PGPASSWORD=postgres

# Or a single connection string (takes precedence if set)
# DATABASE_URL=postgres://user:password@localhost:5432/huntercrm

# App
PORT=3100
JWT_SECRET=change-this-to-a-long-random-secret
