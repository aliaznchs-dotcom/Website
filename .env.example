# Postgres connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=nexustrade
PGUSER=postgres
PGPASSWORD=postgres

# Or a single connection string (takes precedence if set)
# DATABASE_URL=postgres://user:password@localhost:5432/nexustrade

# App
PORT=3000
JWT_SECRET=change-this-to-a-long-random-secret
